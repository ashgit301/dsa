package main

import (
	//"gopaddle/leetcode/linkedList"
	//array "gopaddle/leetcode/array"
	//greedy "gopaddle/leetcode/greedy"
	// backtrack "gopaddle/leetcode/backtrack"
	//strings "gopaddle/leetcode/strings"
	stack "gopaddle/leetcode/stack"
	//"container/list"
	//"compress/gzip"
	//"fmt"
	//tree "gopaddle/leetcode/tree"
)
 func main(){
	//node
	// minIn := 100000000
	// var result []int
	// root := tree.Node{1, nil, nil}
	// root.Left = &tree.Node{2, nil, nil}
	// root.Right = &tree.Node{3, nil, nil}
	// root.Left.Left = &tree.Node{4, nil, nil}
	// root.Left.Right = &tree.Node{5, nil, nil}
	// root.Right.Left = &tree.Node{6, nil, nil}
	// root.Right.Right = &tree.Node{7, nil, nil}
	//linkedList
	// head1 := linkedList.NewNode(1, linkedList.NewNode(5, linkedList.NewNode(7, linkedList.NewNode(9, nil))))
	// head2 := linkedList.NewNode(2, linkedList.NewNode(4, linkedList.NewNode(6, linkedList.NewNode(8, nil))))
	//ans := linkedList.Reverse(head)
	//ans := linkedList.MiddleElement(head)
	//ans := linkedList.MergeLinkedList(head1, head2)
	// a := [][]int{  
	// 	{1, 1, 1, 1} ,   /*  initializers for row indexed by 0 */
	// 	{1, 0, 1, 1} ,   /*  initializers for row indexed by 1 */
	// 	{1, 1, 0, 1},   /*  initializers for row indexed by 2 */
	// }
	// a := [][]int{  
	// 	{1, 2, 3},   /*  initializers for row indexed by 0 */
	// 	{5, 6, 7} ,   /*  initializers for row indexed by 1 */
	// 	{9, 10, 11},   /*  initializers for row indexed by 2 */
	// }
	//_ = a
	//array.MinHeap()
	// a := []int{1,3,10,5,8,5}
	// b := []int{2,4,6,7,9,9}
	//b := []int{1,2,3,4,9,11}
	//b := []int{2, 3, 5, 6}
	// s := "ccad"
	//stockPrice := []int{1,2,3}
	// backtrack.Subsetsum2(stockPrice)
	//strings.LongestCommonPrefix([]string{"geeksforgeeks", "geeks", "geek", "geezer"})
	// strings.ZAlgorithm("hello","ll")
	//strings.WordWrap("Geeks for Geeks presents word wrap problem", 15)
	s := []int{2,1,5,6,2,3}
	//strings.IPAddress(s)
	stack.HistogramStack(s)
	//array.Max_And_Min(a)
	//array.GridUniquePath(3,3)
	//array.SetZeroes2(a)
	//array.PascalTriange(5)

	//ab := [][]int{{1,3},{2,6},{8,10},{15,18}}
	//ab := []int{7,6,4,3,1}
	// array.Permutation(ab)
	//array.Kadane(ab)
	//array.StockBuyAndSell2(ab)
	//array.MergerIntervals(ab)
	//fmt.Println(ans.Val)
	//linkedList.Display(ans)
    //root.Right.Right.Left = &tree.Node{1,nil,nil}
	//tree.InorderTraversal(&root, &result)
	//tree.PreorderTraversal(&root, &result)
	//tree.PostorderTraversal(&root,&result)
	//tree.LeftView(&root,&result)
	//tree.RightView(&root,&result)
	//tree.BottomView(&root,&result)
	//tree.TopView(&root,&result)
	//tree.LevelOrder(&root,&result)
	//tree.ReverseLevelOrder(&root,&result)
	//tree.DiagnolTraversal(&root,&result)
	//ans := tree.Height(&root)
	//ans := tree.Diameter(&root)
	//ans := tree.MaxPathSum(&root)
	//ans:= tree.MinimumVal(&root, &minIn)
	//ans := tree.SumTree(&root)
	// fmt.Println(ans)
	// fmt.Println(result)
	
 }